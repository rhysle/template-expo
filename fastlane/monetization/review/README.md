# Apple IAP Review Screenshots

Place the private App Review screenshots for subscriptions and non-consumable purchases in this
directory, then set each product's `appleReviewScreenshotPath` in
`src/configs/monetization.ts`.

The same image can be referenced by multiple products. These files demonstrate the purchase flow
to App Review and are separate from the public listing screenshots under `fastlane/ios/screenshots/`.
