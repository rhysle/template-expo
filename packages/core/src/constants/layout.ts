/** Maximum width for centered content on tablets and other wide mobile layouts. */
export const TABLET_CONTENT_MAX_WIDTH = 720

/**
 * Conservative fallback used before the custom tab bar is measured and for root
 * overlays above native tabs, whose height Expo Router does not expose.
 */
export const TAB_BAR_HEIGHT = 88
