import { TabPlaceholderScreen } from '@shared/core/components/base'

export default function ToneGeneratorScreen() {
  return (
    <TabPlaceholderScreen description="Adjust the sound frequency used for speaker cleaning." />
  )
}
